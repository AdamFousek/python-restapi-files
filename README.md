# python-restapi-files

Flask app with REST API to show dicts and files in path

To run project:

pip install -r requirements.txt
python main.py
Open index.html in browser or use Postman for communicate with server.
